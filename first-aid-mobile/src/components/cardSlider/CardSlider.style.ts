import {StyleSheet} from 'react-native';
import {size} from '@components';

export const styles = StyleSheet.create({
  wrapper: {},

  container: {},

  titleContainer: {
    marginBottom: size.m,
    flexDirection: 'row',
  },
});
