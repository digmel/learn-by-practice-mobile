import React, {FC} from 'react';
import {TLoginScreenViewProps} from './LoginScreen.type';
import {LoginScreenView} from './LoginScreen.view';

export const LoginScreen: FC<TLoginScreenViewProps> = () => {
  return <LoginScreenView />;
};
